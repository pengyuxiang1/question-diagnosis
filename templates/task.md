# {问题标题}（{YYYY-MM-DD}）

> 🔁 **引导机制 —— 每个动作前先跑，不要凭记忆推进**
>
> ```bash
> python3 .codebuddy/skills/question-diagnosis/scripts/flow_guide.py \
>   .codebuddy/skills/question-diagnosis/tasks/{YYYY-MM-DD-问题slug}/task.md
> ```
>
> 它会读本文件、返回：**当前阶段 / 下一步做什么 / 怎么做 / 证据缺口清单**。
> 定稿或下结论前加 `--check` 自检：**退出码 1 = 有证据缺口，不许下结论**。
>
> 🔒 本阶段 `can_write_code = false` —— 定位期间禁止修改业务代码（修复是定位完成之后的事）。
> ⚠️ 标 🟢/🔴 前必须有证据（出处 + 原文 + 复查方式）；`[推断]` 必须与证据分开标注，**严禁当结论写进回复**。

## 问题事实

> Step 0：**只收事实，禁止写根因猜想**。

- **现象**：
- **时间窗**：
- **环境**：
- **用户原话**：
- **相关 ID**（trace / conversation / 订单号 / uid）：

## Round 1

> 每条假设 = 一句话**可证伪**的表述 + 验证方法 + 预期证据（**先写死判定标准**）。
> 禁止空泛假设（"可能是网络问题" → 必须落到：查哪个连接、哪段日志能证实）。

### H1 假设：{一句话可证伪的表述}
- 状态：🟡 未验证
- 验证方法：{查哪份日志 / 哪个索引集 / 哪段代码 / 哪个文件}
- 预期证据：{看到什么 = 证实；看到什么 = 证伪}
- 实际证据：
- 结论：

### H2 假设：{...}
- 状态：🟡 未验证
- 验证方法：
- 预期证据：
- 实际证据：
- 结论：

## Step 4 结论

> 全部假设判定后再写。根因链**每一跳都要带证据出处**；证据链有缺口的结论必须标「待补证」，不得当根因。
> 排除项清单：哪些假设被证伪 + 依据。

（待填写）

## Step 5 归档判断

- 会复现 / 可能再遇 → 提炼为 `references/{domain}-{issue}.md`，并更新 `references/knowledge-map.md`
- 一次性且确定不会复现 → 不归档
