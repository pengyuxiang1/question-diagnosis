---
name: question-diagnosis
description: 以"假设—验证"循环驱动的问题定位工作流，含引导脚本与门禁 hook，强制"先交证据、再下结论"。适用于排查任意代码/服务故障：报错、异常、不生效、没数据、数据不对、返回空、超时、卡住、消息延迟、任务未完成、配置未生效等。触发词：问题定位、排查、定位问题、debug、调试、找原因、什么原因、报错、异常、不生效、没数据、数据不对、没返回、返回空、超时、卡住、查日志、查链路、查 trace、查数据、查配置、复现、根因。
---

# 问题定位（question-diagnosis）

> 以「假设—验证」循环驱动的通用问题定位工作流。核心主张：**排查必须走流程，结论必须有证据**。
>
> 本 skill 把排查协议从提示词里搬进了三样东西：**状态文件**（记忆）、**引导脚本**（方向）、**门禁 hook**（执行）——长任务里，写在 SKILL.md 里的软约束会被上下文稀释，这三样不会。

## 🚀 起手式（开工前 30 秒）

### 目录约定

```
question-diagnosis/
├── SKILL.md
├── references/          # 知识 & 案例文档（归档后在此，命名 {domain}-{issue}.md）
├── tasks/               # ⭐ 定位工作区：每个问题一个目录
│   └── YYYY-MM-DD-{slug}/task.md   # task.md 驱动排查（从 templates/ 复制）
├── templates/
│   └── task.md          # 状态文件模板（顶部钉引导指令）
└── scripts/
    └── flow_guide.py    # 引导脚本（零依赖，只读）
```

### 🔁 引导机制（核心：不要凭记忆推进，每个动作前先跑）

> ⚠️ **为什么需要它**：协议写在 SKILL.md 里是**软约束**——长任务后 AI 会忘记「要验证、要证据」，转而凭猜测给结论。引导脚本把「下一步该做什么」变成**每次现场拉取的状态**，而不是上下文里会沉底的一段话。

```bash
# 每个动作前先跑：读 task.md 算出当前阶段与下一步（唯一必记命令）
python3 .codebuddy/skills/question-diagnosis/scripts/flow_guide.py <task.md路径>

# 下结论前自检：退出码 1 = 有证据缺口，不许给根因
python3 .codebuddy/skills/question-diagnosis/scripts/flow_guide.py <task.md路径> --check

# 看所有进行中的排查
python3 .codebuddy/skills/question-diagnosis/scripts/flow_guide.py --list
```

**返回的引导字段**：`stage`（当前阶段）/ `progress`（假设判定进度）/ `next`（下一步动作）/ `how`（怎么做）/ `gaps`（证据缺口清单）/ `can_write_code`（是否允许改代码）/ `reminder`（硬约束）。

**`--check` 会拦的三类问题**（这就是把软约束硬化的部分）：
1. 标 🟢 已证实 / 🔴 已排除的假设，证据段落**没有可复查出处**（需含 `文件:行号` / `[来源 标注]` / `trace_id` / 代码块 之一）
2. 已判定的假设**缺「验证方法」或「预期证据」**（判定标准必须事先写死，不许事后补）
3. 写了结论但**没有任何 🟢 假设支撑**（结论无证据基础）

> 📌 定位期间 `can_write_code = false`——**禁止修改业务代码**，门禁 hook 会拦截。

## 排查工作流协议（刨根问底版）

> ⚠️ **最常见的失败模式**：把「现象恢复正常」当结论（"自愈"）；把「A 侧无日志」直接归因 A 侧不响应——根子都是**先有结论后找证据**。
> **推论只是待验证假设，不是结论。**每条结论都要有日志原文 / DB 值 / 代码行支撑，标得出处。
>
> 📌 每个 Step 开始前，先跑一次「起手式」里的 `flow_guide.py`，按它的返回推进。

### Step 0：搜集问题（只收事实）

新建 `tasks/YYYY-MM-DD-{slug}/task.md`（**从 `templates/task.md` 复制**，顶部自带引导指令，不要手写空文件）。

现象 / 时间窗 / 环境 / 用户原话 / 相关 ID（trace、会话号、订单号）→ 记入「问题事实」节。**此步禁止写根因猜想。**

### Step 1：分析疑点（列出全部候选解释）

基于代码 / 架构 / 已有证据列出所有候选假设，每条标注证据状态：🟡 未验证 / 🟢 已证实 / 🔴 已排除。只列**能被具体证据证实或证伪**的假设，不列空泛猜测（"可能是网络问题"必须落到：查哪个连接、哪段日志能证实）。

### Step 2：写 task.md（每个疑点一条 task）

每条 task 必须含：

- **假设**：一句话可证伪的表述
- **验证方法**：可执行动作（查哪份日志 / 哪个索引集 / 哪段代码 / 哪个文件）
- **预期证据**：看到什么 = 证实，看到什么 = 证伪（先写死判定标准）
- **实际证据**（排查后填）：原文 + 出处（`文件:行号` / 索引集 + 时间窗 / trace_id）
- **结论**：🟢 证实 / 🔴 证伪 / 🟡 证据不足

禁止"再想想"型 task。task.md 在排查中持续更新，作为排查过程留痕。

### Step 3：按 task 逐条排查

只信真实证据（日志原文、DB 值、代码行）。证据必须带出处。中间推理可标注 `[推断]`，但与证据区分开。**严禁把 `[推断]` 当结论写进回复。**

### Step 4：复盘（找到没找到都要做）

全部 task 完成后汇总证据链：

- 已定位 → Step 5
- 未定位 → 基于**本轮新证据**回到 Step 1 重开一轮（旧假设标 🔴 并写明证伪依据，更新 task.md 追加第二轮）；**禁止无新证据地重复猜测、换说法重说、或把未证实假设当结论**
- 证据链有缺口的结论不得当根因（缺口 = 关键跳转无日志 / 无数据支撑，必须标"待补证"）

### Step 5：输出定位推论文档（携带全部真实证据）

根因链（每跳都有证据）+ 关键证据原文（时间戳 + 出处）+ 排除项清单（哪些假设被证伪、依据）+ 解决方案（分级：根治 / 防御 / 止血 / 检测）。复盘中的弯路也写入（为什么误判、正确姿势），供下次借鉴。

### Step 6：归档

- **会复现 / 可能再遇** → 提炼为 `references/{domain}-{issue}.md` 案例（现象 → 判定特征 → 证据链 → 根因 → 方案 → 复盘 → 可复用工具），同步更新 `references/knowledge-map.md`
- **一次性且确定不会复现** → 不归档

> 旧的通用原则仍有效，内嵌于上述流程：先读代码再查日志；列原因排优先级；逐个排除不跳步；结论必须有证据。

## 三层路由：把你的环境接进来

上层协议是通用的；「查什么、用什么查」取决于你的环境。本 skill 用三层 map 承接：

| 层 | 文件 | 回答什么问题 |
|----|------|-------------|
| 路由 1 | `references/tool-map.md` | 这个现象该用什么工具查？ |
| 路由 2 | `references/knowledge-map.md` | 这个坑以前踩过吗？ |
| 路由 3 | `references/{domain}-{issue}.md` | 这个领域的专属排查手册 |

三个文件初始为骨架模板，**按你自己的系统填写**。填得越厚，排查越快——这也是本 skill "越用越好"的部分。

### 路由 1：现象 → 工具

拿到问题不要凭直觉查，先花 10 秒确认「该查什么、有什么工具」。打开 `references/tool-map.md`（骨架已给示例行）：

| 问题现象 | 先查什么 | 工具 |
|---------|---------|------|
| 服务报错 / 异常 | 服务日志 | （你的日志平台查询工具） |
| 一次请求的完整链路 | trace | （你的 trace 查询方式） |
| 成功率 / 耗时 / 请求量 | 指标 | （你的监控平台） |
| 谁操作过（审计溯源） | 流水 / DB | （你的审计查询方式） |
| 业务数据对不对 | DB | （你的 DB 客户端） |
| 配置对不对 | 配置中心 | （你的配置平台工具） |

### 路由 2：现象 → 历史经验

**查工具前先看有没有人踩过这个坑。**`references/knowledge-map.md`（现象 → 知识文档索引）命中关键词时，直接读对应文档获取历史根因 / 定位流程 / 验证命令，避免从零排查。每次沉淀新经验后必须同步更新该 map。

### 路由 3：领域手册

按问题现象判断领域，加载对应的 `references/{domain}-*.md` 文档（例：`pay-refund-timeout.md`、`gateway-502.md`）。领域手册建议包含：**关键词 → 路由表（问题类型 → reference） → 代码位置 → 快捷脚本**。

## 🔒 运行时知识沉淀（本 skill 的记忆约定）

> 本 skill 使用过程中积累的经验（排查案例、工具地图、踩坑记录）的存放位置。**未来会话据此知道经验往哪写、从哪读**。

```
知识目录：references/（skill 内部）
├── references/{domain}-{issue}.md   # 归档案例（Step 6 产出）
├── references/knowledge-map.md      # 现象 → 案例索引（归档时同步更新）
└── references/tool-map.md           # 现象 → 工具决策（踩坑后回填）
```

三条约定：

1. **新经验追加，不覆盖**：每次归档（Step 6）新增一个案例文件 + 在 knowledge-map 加一行，旧内容永不删改
2. **skill 执行前先查索引**：排查任何问题前，先扫一眼 `knowledge-map.md` 命中历史经验，再决定是否从零开始
3. **时效自检**：references 下的知识文件都带 `verified / stale_after` 时效标记——检索到过期内容时**先重新核对再用**，不要直接当可信知识

⚠️ **升级注意**：若你通过 `git pull` / 重新安装更新本 skill，`references/` 下的案例是**你自己的积累**，不在上游仓库里——更新前备份 `references/` 与 `tasks/`（进行中的排查）。若希望知识与 skill 完全解耦（升级永不丢失），把上述三个路径整体外移到 `~/.question-diagnosis/` 并同步修改本节声明即可。

## 门禁 hook：怎么装

`hooks/diagnosis-gate.js` 是与本 skill 配套的门禁脚本，覆盖两类问题：

| 事件 | 拦截内容 |
|------|---------|
| **PreToolUse** | 定位阶段（`can_write_code=false`）偷写业务代码 → deny；写 task.md 等留痕区 → 放行 |
| **Stop** | 该跑引导没跑 / 结论不合规（证据无出处、假设未收敛就下结论）→ 提醒（可升级为阻断） |

**安装**（以 CodeBuddy 为例，写入 `.codebuddy/settings.json`）：

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^(Edit|MultiEdit|Write|NotebookEdit)$",
        "hooks": [
          { "type": "command", "command": "node .codebuddy/hooks/diagnosis-gate.js", "timeout": 10 }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          { "type": "command", "command": "node .codebuddy/hooks/diagnosis-gate.js", "timeout": 15 }
        ]
      }
    ]
  }
}
```

**四条纪律**（写 hook 的通用原则，详见 skillforge 的 hook-spec）：

1. **hook 常挂，脚本内判断**——不按阶段插拔配置；脚本读 task.md 的阶段决定拦不拦
2. **fail-open**——脚本任何异常都放行，绝不因门禁自身出错阻塞 AI
3. **默认只提醒，按需阻断**——`systemMessage` 零成本；`continue: false` 要付一轮 LLM 调用
4. **改配置前先备份**；留一个环境变量做总开关（`DIAG_GATE_OFF=1`）

> ⚠️ Hook 语法各家 Agent 不通用（CodeBuddy / Claude Code / Codex 各不相同），照搬会静默失效。装之前先查目标 Agent 的官方文档。

## 设计原则（为什么这么做）

1. **记忆在磁盘上，不在上下文里**——状态文件落盘，压缩、换会话、换模型都不怕
2. **判定标准必须能机器校验**——"证据充分"是虚的，"证据段含 `文件:行号`"是实的；写不出检查代码的判定，门禁拦不住
3. **约束落在产物上**——不靠 AI 自觉，靠程序对 task.md 逐行检查
4. **先上机制，再谈优化**——先让流程跑起来，观察真实缺口，再决定拦什么
